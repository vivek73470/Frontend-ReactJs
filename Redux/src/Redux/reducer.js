

// const reducer = (oldState, action)=>{ 
//     // console.log(oldState,action)
//     switch (action.type){
//         case "ADD":
//             // creating new object
//             // we are spreading the old state and increment the count by accessing old count and adding the no. that we get through payload
//             return {...oldState, count:oldState.count + action.
//             payload };
//                // and it goes back to store
//             case "REDUCE":
//                 return {...oldState, count:oldState.count - action.
//                 payload };
//         default:
//             return oldState;
//     }
// }
// export  {reducer};